using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemSlot : DropItem
{
    // Start is called before the first frame update
    void Start()
    {
        CheckChild();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
